<%= form_for @article do |f| %>

  <% if @article.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@article.errors.count, "error") %> prohibited
        this article from being saved:
      </h2>
      <ul>
        <% @article.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.label :title, id: 'input-title' %></br>
  <%= f.text_field :title, id: 'input-title-text' %></br>

  <%= f.label :body, id: 'input-body' %></br>
  <%= f.text_area :body, id: 'input-body-text' %></br>

  <%= f.submit id: 'submit-button' %></br>

<% end %>
